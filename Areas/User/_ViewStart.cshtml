@{

    if (ViewContext.ActionDescriptor.RouteValues["area"] == "User")
    {
        Layout = "_UserLayout";
    }
    else
    {
        Layout = "_Layout"; // Specify the default layout for non-admin areas
    }
}
